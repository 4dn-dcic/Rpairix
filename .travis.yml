sudo: false
language: R
cache: packages

r_github_packages:
  - jimhester/covr
  - r-lib/testthat

r:
  - release
  - devel
  - oldrel

bioc_packages: 
  - GenomicRanges
  - InteractionSet

# Upload CodeCov information
after_success:
  - tar -C .. -xf $PKG_TARBALL
  - Rscript -e 'covr::codecov()'
